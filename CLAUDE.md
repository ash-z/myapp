# myapp

`invite-src/` is Sai Susmita and Ashish's wedding invitation; `invite-src/HANDOFF.md` has everything about it.

## Branches
- `golden` is the live site guests see (GitHub Pages, `/docs`). Never commit to it directly.
- Work on `claude/add-threejs-library-ogt1s7`, and share the dev previews (`invite-src/build/artifact-dev.html`, `artifact-dev-friends.html`).
- Promote dev to golden only when the user asks (steps in HANDOFF.md, "Golden and dev").

`docs/index.html` is generated by `invite-src/build.py`; don't edit it by hand.

Two versions are built from one source: relatives (`/`) and friends (`/friends/`, which adds the 28 Oct Haldi / Pellikuturu / Mehendi & Sangeet page). Friends-only markup goes between `<!--friends-->` lines in `invite-src/body.html`; it must never reach the relatives' version, not even in comments in the shared `style.html` / `app.js` (they ship in both pages).
